using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class ButtonNavigation : MonoBehaviour
{
    public string FirstButton;

    public GameObject MenuPanel, OptionsPanel, LevelsPanel;

    
  /*  void Start()
    {
        GameObject.Find(FirstButton).GetComponent<Button>().Select();
    } */

   /* public void Update()
    {
        if (MenuPanel.activeSelf == true)
        {
            GameObject.Find(FirstButton).GetComponent<Button>().Select();
            Debug.Log("MenuPanel is active");
        }
        else if (OptionsPanel.activeSelf == true)
        {
            GameObject.Find(FirstButton).GetComponent<Button>().Select();
            Debug.Log("OptionsPanel is active");
        }
        else if (LevelsPanel.activeSelf == true)
        {
            GameObject.Find(FirstButton).GetComponent<Button>().Select();
            Debug.Log("LevelsPanel is active");
        }
    }*/
   
}
